<div class="container min-vh-100">
  <div class="row">
    <div class="col-md-4 offset-md-4 py-5 my-5">
      <h1 class="text-center p-3 fw-bold">User Profile</h1>
      <div class="card text-center shadow">
        <div class="card-header">
          <h3>{{user.first_name}} {{user.last_name}}</h3>
        </div>
        <div class="card-body">
          <h5 class="card-title fw-bold">Email: {{user.email}}</h5>
          <h5 class="cart-text fw-bold">CartID: {{user.cart}}</h5>
          <h5 class="card-text fw-bold">Role: {{user.role}}</h5>
        </div>
        <div class="card-footer text-muted">
          <h5 class="fw-bold">UserID: {{user._id}}</h5>
        </div>
      </div>
    </div>
  </div>
</div>